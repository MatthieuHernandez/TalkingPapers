<li>
  <%= render partial: 'users/profil_picture', locals: {user: user}%>
  <%= link_to user.name, user, "data-turbo":false %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete, data: { confirm: "You sure?" } %>
  <% end %>
</li>