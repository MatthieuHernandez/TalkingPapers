<li>
  <div>
    <%= kramdown_title(article.title) %>
  </div>
  <% if current_user == @user.id %>
    <%= link_to "View private notes", article_path(:id => article.id) %> |
  <% end %>
  <%= link_to "View public notes", article_path(:id => article.id, :query => "public") %>
</li>