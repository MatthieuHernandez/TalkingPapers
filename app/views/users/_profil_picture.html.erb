<% if user.avatar.attached? %>
  <img alt="<%= user.name %>" src="<%= url_for(user.avatar)%>" width="80" height="80">
<% elsif user.picture_link? %>
  <img alt="<%= user.name %>" src="<%= user.picture_link %>" width="80" height="80">
<% else %>
  <%= gravatar_for user, size: 50 %>
<% end %>